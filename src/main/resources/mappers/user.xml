<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.project.noonee.domain.user.UserRepository">
	<insert id="save" parameterType="com.project.noonee.domain.user.User">
		insert into
			user_mst
		values(
			0,
			#{user_name},
			#{user_email},
			#{user_id},
			#{user_password},
			#{user_roles},
			#{user_provider},
			now(),
			now()
		)
	</insert>
	
	<select id="findUserByUsername" parameterType="String" 
		resultType="com.project.noonee.domain.user.User">
		select		
			user_name,
			user_password		
		from
			user_mst
		where
			user_name = #{username}
	</select>
</mapper>